generator client {
    provider = "prisma-client-js"
    output   = "../generated/prisma-client"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model Post {
    domain String @id
    url    String

    content    String
    updatedAt  DateTime?
    authorName String?
}

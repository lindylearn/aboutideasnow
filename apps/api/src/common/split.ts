export function getPostParagraphs(text: string) {
    // const paragraphs = text
    //     .split("\n\n")
    //     .map((p) => p.trim())
    //     .filter((p) => p.length > 10).slice(0, 50);

    return [text];
}
